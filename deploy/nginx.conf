worker_processes auto;

events {
    worker_connections 2048;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 50M;

    # HTTP/1.1 keepalive for upstream to prevent connection resets
    proxy_http_version 1.1;
    proxy_set_header Connection "";

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # Disable access log to avoid logging visitor IP addresses
    access_log off;

    # Proxy cache for thumbnails and static API responses
    proxy_cache_path /tmp/nginx_cache levels=1:2 keys_zone=api_cache:10m max_size=500m inactive=1h;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=search:10m rate=30r/s;
    limit_req_zone $binary_remote_addr zone=api:10m rate=60r/s;
    limit_req_zone $binary_remote_addr zone=upload:10m rate=10r/m;

    # Docker DNS resolver — re-resolve dynamically so container
    # restarts don't leave nginx pointing at a stale IP.
    resolver 127.0.0.11 valid=10s ipv6=off;

    # HTTP — redirect to HTTPS (+ ACME challenges)
    server {
        listen 80;
        server_name _;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS — primary site (Epstein Files)
    server {
        listen 443 ssl http2;
        server_name unredact.org www.unredact.org;

        ssl_certificate /etc/letsencrypt/live/unredact.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/unredact.org/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers off;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # API routes — proxy to archiver
        location /api/ {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://archiver:4000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_read_timeout 300s;
        }

        # File uploads
        location /api/upload {
            limit_req zone=upload burst=10 nodelay;
            client_max_body_size 512M;
            proxy_pass http://archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_request_buffering off;
            proxy_read_timeout 600s;
        }

        # Torrent downloads
        location ~ ^/api/datasets/\d+/torrent$ {
            limit_req zone=api burst=5 nodelay;
            proxy_pass http://archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Search — tighter rate limit, buffered
        location /api/documents/search {
            limit_req zone=search burst=10 nodelay;
            proxy_pass http://archiver:4000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_buffering on;
            proxy_buffer_size 128k;
            proxy_buffers 4 128k;
        }

        # Document content — streaming (PDFs, videos, etc)
        location ~ ^/api/documents/.+/content$ {
            limit_req zone=api burst=30 nodelay;
            proxy_pass http://archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 600s;
            add_header Cache-Control "public, max-age=604800";
        }

        # Frontend — Next.js
        location / {
            proxy_pass http://frontend:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # Override Next.js static cache headers to prevent Cloudflare
            # from caching empty pages for a year
            proxy_hide_header Cache-Control;
            add_header Cache-Control "public, s-maxage=60, stale-while-revalidate=300" always;
        }

        # Static assets — long cache
        location /_next/static/ {
            proxy_pass http://frontend:3000;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }
    }

    # HTTPS — Iran Uncensored
    server {
        listen 443 ssl http2;
        server_name iran.unredact.org;

        # Use the same wildcard/SAN cert, or a separate cert for the subdomain.
        # For now, fall back to the primary cert (works if it covers *.unredact.org
        # or if certbot has issued a cert for iran.unredact.org).
        ssl_certificate /etc/letsencrypt/live/unredact.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/unredact.org/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers off;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # API routes — proxy to Iran archiver
        location /api/ {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://iran-archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_buffering off;
            proxy_read_timeout 300s;
        }

        # File uploads — larger limit for video
        location /api/upload {
            limit_req zone=upload burst=10 nodelay;
            client_max_body_size 2048M;
            proxy_pass http://iran-archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_request_buffering off;
            proxy_read_timeout 600s;
        }

        # Torrent downloads
        location ~ ^/api/datasets/\d+/torrent$ {
            limit_req zone=api burst=5 nodelay;
            proxy_pass http://iran-archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Search
        location /api/documents/search {
            limit_req zone=search burst=10 nodelay;
            proxy_pass http://iran-archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Document content — long cache, streaming
        location ~ ^/api/documents/.+/(content|thumbnail) {
            limit_req zone=api burst=30 nodelay;
            proxy_pass http://iran-archiver:4000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 600s;

            add_header Cache-Control "public, max-age=604800";
        }

        # Frontend — Iran Next.js
        location / {
            proxy_pass http://iran-frontend:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Static assets — long cache
        location /_next/static/ {
            proxy_pass http://iran-frontend:3000;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }
    }
}
